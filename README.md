# Fortran LU分解与线性方程组求解器

20260202

一个高性能、稳定的Fortran库，用于执行LU矩阵分解并求解线性方程组。采用部分选主元算法，确保数值稳定性，并包含完整的测试套件和应用示例。

## ✨ 特性

- **高性能 LU 分解**：针对稠密矩阵优化的分解算法。
- **数值稳定**：实现了部分选主元（Partial Pivoting），有效处理病态矩阵。
- **完整求解器**：提供前向替换与后向替换，一站式求解 `Ax = b`。
- **丰富工具**：包含矩阵生成器、范数计算、残差检查等工具函数。
- **模块化设计**：代码结构清晰，便于集成与扩展。
- **全面测试**：包含单元测试、性能测试及实际应用案例（如电路分析）。

## 📁 项目结构

```
fortran-lu-solver/
├── src/                    # 源代码
│   ├── ludecomp_m.f90     # LU分解核心模块
│   ├── linear_solvers.f90 # 线性求解器接口
│   └── matrix_utils.f90   # 矩阵工具函数
├── test/                  # 测试代码
│   ├── test_basic.f90    # 基础功能测试
│   └── test_performance.f90 # 性能基准测试
├── apps/                  # 应用示例
│   └── circuit_solver.f90 # 电路分析应用
├── docs/                  # 文档
├── makefile               # 构建脚本
├── generate_structure.py  # 生成目录树结构程序
├── PROJECT_STRUCTURE.md   # 目录树结构
├── license                # 许可证/开源协议
├── readme.md              # 项目入口
├── numerics.md            # 数值行为与可重复性说明
├── contributing.md        # 贡献指南 
├── code_of_conduct.md     # 行为准则
├── .gitignore             # Git忽略规则
└── changelog.md           # 版本记录
```

## 🚀 快速开始

### 前置要求

- Fortran 编译器（如 `gfortran`）
- `make` 构建工具

### 克隆与构建

```bash
git clone https://gitee.com/fluman2024/fortran_lu_solver.git
cd fortran-lu-solver
# 编译所有目标
make all
# 运行测试
make test
# 运行电路分析应用
./bin/circuit_solver
# 清理构建文件
make clean
```

### 基本用法

```fortran
program demo
  use linear_solvers
  implicit none
  real, dimension(3,3) :: A
  real, dimension(3) :: b, x
  integer :: ierr

  ! 初始化矩阵和向量
  A = reshape([2.0, 1.0, 1.0, &
               4.0, 3.0, 3.0, &
               8.0, 7.0, 9.0], [3,3])
  b = [1.0, 2.0, 3.0]

  ! 求解方程组
  call solve_linear_system_with_pivot(A, b, x, ierr)

  if (ierr == 0) then
    print *, '解向量 x:', x
  else
    print *, '求解失败，错误代码:', ierr
  end if
end program demo
```

## 📖 API文档摘要

### 主要子程序

- `ludeco_with_pivot(A, pivot, ierr)`: 对矩阵A进行带选主元的LU分解。
- `solve_linear_system_with_pivot(A, b, x, ierr)`: 求解方程组 Ax = b 的完整接口。
- `matrix_residual(A, x, b)`: 计算残差范数 \|\|Ax - b\|\|，用于验证解的正确性。

（更多API详情请查阅各模块源码或 `docs/` 目录下的详细文档。）

## 🤝 如何贡献

我们欢迎任何形式的贡献！请查阅 [CONTRIBUTING.md](CONTRIBUTING.md) 了解贡献流程和代码规范。

## 📄 许可证

本项目基于 MIT 许可证开源 - 详见 [LICENSE](LICENSE) 文件。

## 📊 项目状态

![Gitee Stars](https://gitee.com/fluman2024/fortran_lu_solver/badge/star.svg)
